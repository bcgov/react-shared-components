import { Meta, Story } from "@storybook/addon-docs/blocks";
import { Table } from "./Table";

<Meta title="Table" component={Table} />

# Table

The table lets you aggregate a huge amount of data and present it in a clear and orderly way.

## API

| Prop          | Type             | Required | Description                                             | Default |
| ------------- | ---------------- | -------- | ------------------------------------------------------- | ------- |
| heading       | string/object    | false    | value representing heading to display within table      | ""      |
| elements      | array of objects | false    | content of the table                                    | []      |
| styling       | string           | false    | represents the styling you wish to apply for your table | ""      |
| elementStyles | object           | false    | represents row and column styles you wish to apply      | {}      |

### Elements API

The elements array contains objects of the following structure:

| Prop        | Type          | Required | Description                                                  | Default |
| ----------- | ------------- | -------- | ------------------------------------------------------------ | ------- |
| name        | string/object | true     | the key of the key:value pair in the table                   |         |
| value       | string/object | true     | the value of the key:value pair in the table                 |         |
| isValueBold | boolean       | false    | represents if you want your values to be bolded in the table | false   |
| isNameBold  | boolean       | false    | represents if you want your keys to be bolded in the table   | false   |
| isEmptyRow  | boolean       | false    | represents if you want to have an empty row with hr tag      | false   |

<br />

## Without Header

A table without a header.

<Story name="Without Header" id="table--without-header"></Story>

```bash
const elements = [
  { name: "BCeID", value: "bobross42" },
  {
    name: "Last Name",
    value: "Ross"
  },
  { name: "First Name", value: "Bob" },
  { name: "Email Address", value: "bob.ross@example.com" }
];

<Table elements={elements} />;
```

## With Header

A table with a header.

<Story name="With Header" id="table--with-header"></Story>

```bash
<Table heading="BCeID Info" elements={elements} />;
```

## With Blue Stripe

A table with blue stripes.

<Story name="With Blue Stripe" id="table--with-blue-stripe"></Story>

```bash
<Table elements={elements} styling="blue-stripe" />
```

## With Bold Values

A table with bolded values.

<Story name="With Bold Values" id="table--with-bold-values"></Story>

```bash
const tableDataWithBoldValues = [
  { name: "Key", value: "Value", isValueBold: true }
];

<Table elements={tableDataWithBoldValues} />
```

## With Bold Names

A table with bolded names.

<Story name="With Bold Names" id="table--with-bold-names"></Story>

```bash
const tableDataWithBoldNames = [
  { name: "Key", value: "Value", isNameBold: true }
];

<Table elements={tableDataWithBoldNames} />
```

## With Fees

A table for displaying fees.

<Story name="With Fees" id="table--with-fees"></Story>

```bash
const feesData = [
  { name: "Some Fees:", value: "$100.00", isValueBold: true },
  { name: "Some More Fees:", value: "$10.00", isValueBold: true }
];

<Table elements={feesData} />
```
